export const publicRoutes = ["/"];

export const authRoutes = ["/login", "/register", "/forgetPass", "/Signin"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/torus";
